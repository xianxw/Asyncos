[package]
name = "all"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "native-tls"] }
scraper = "0.18"
tokio = { version = "1", features = ["full"] }
sysinfo = "0.30"
