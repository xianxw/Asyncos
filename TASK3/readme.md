## 异步化改造思路

### 思路一：阻塞点异步化（轻量改造）

将当前代码的阻塞点改造为状态机形式。当前代码的阻塞点分布在：

- `sdmmc.rs` 的 `wait_until` 忙等
- `send_cmd` 中的等待逻辑
- `init` 中的等待逻辑
- 读写块函数中的等待逻辑

对于硬件协议部分可以保留。

### 思路二：深度异步化改造（中断 + DMA）

如果需要更彻底的异步化，还需要把以下机制接进去：

- 中断唤醒
- 完成通知
- 可能的 DMA 传输路径
